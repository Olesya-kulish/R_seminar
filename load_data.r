## Load data / helper runner for analyses
## Run `Rscript scripts/make_city_summary.R` to recreate the city-level summary table

if (interactive()) {
  message("To generate city summary table, run: scripts/make_city_summary.R")
}
